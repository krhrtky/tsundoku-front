import React from 'react';
import { storiesOf } from '@storybook/react';
import { RegisterModal } from './RegisterModal';

storiesOf('Organisms/Book', module).add('RegisterModal', () => (
  <RegisterModal />
));
